# v0.5.2 demo evidence overlay

This overlay adds an optional Evidence Workspace demo path on top of v0.5.1/current worktree behaviour.

- Load synthetic evidence for all 195 control contracts.
- Continue to support normal engagement folder scanning.
- Keep synthetic evidence outside the workbench to protect self-scan boundaries.
- Exclude `.demo_manifest.json` from scanner evidence.
- Use deterministic lexical fallback when `rank-bm25` is unavailable.
