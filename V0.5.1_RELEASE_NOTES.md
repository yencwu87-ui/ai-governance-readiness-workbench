# Governance Engine v0.5.1 Release Notes

## Theme

**Canonical Control Contract — one schema for requirement context, testing and challenge.**

## Included

- Per-framework canonical YAML contracts for MAS, ISO/IEC 42001, NIST AI RMF, MGF Agentic and SAFR.
- Workbook-to-contract export utility: `tools/export_control_contracts.py`.
- `Assess with AI` reads the canonical control contract in addition to evidence and advisory knowledge.
- `Challenge My Read` uses the same contract to anchor claims to elements, ToD/ToE steps, failure modes and resolutions.
- MAS requirement semantics remain authoritative from `requirements/mas.yaml`.
- Workbook-derived testing/challenge content remains draft until formally approved.
- Assessment identity includes the canonical control contract artefacts.
- Release status validates per-framework contract files.

## Verification

```text
163 tests passed
30 MAS controls with ToD/ToE
195 total canonical control contracts
link integrity: PASS
contract validation: PASS
```

## Deliberate status

The model assessor is not considered production-validated merely because the control contracts are complete. A fresh evaluation against the active corpus must still pass the release gate.
